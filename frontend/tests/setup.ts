import { initializeAppMetadata } from '@/appMetadata'

initializeAppMetadata({
  version: '0.0.0',
  releaseNotes: [{
    version: '0.0.0',
    releaseDate: null,
    changes: [],
    upgradeNotes: [],
    draft: true,
  }],
})
